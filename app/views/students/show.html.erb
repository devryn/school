<h2><%= students.name %></h2>

<button type="button" class="btn btn-default"><%= link_to "Edit Student", edit_student_path(students) %></button>
<button type="button" class="btn btn-danger"><%= link_to "Delete Student", students, method: :delete, data: { confirm: "Are you sure you want to delete this student?"} %></button>
